import { TurnosView } from './TurnosView'

/**
 * Punto de entrada de la ruta `/turnos` (ver `App.tsx`). El contrato del
 * módulo y la maqueta con datos mockeados viven en `TurnosView.tsx`.
 */
export function TurnosPage() {
  return <TurnosView />
}
